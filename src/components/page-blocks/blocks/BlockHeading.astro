---
const { content, pageFiles } = Astro.props;
const headingLevel = content.headingLevel || 2;
const headings = {
  1: "h1",
  2: "h2",
  3: "h3",
  4: "h4",
  5: "h5",
  6: "h6",
};
const HeadingTag = headings[headingLevel];
---

<HeadingTag set:html={content.text} />
